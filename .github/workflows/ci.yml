name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            packages/transcript-postprocess/pyproject.toml
            constraints-runtime.txt
            constraints-dev.txt
            requirements-cpu.txt
            requirements-cuda.txt
            requirements-dev-cpu.txt
            requirements-dev-cuda.txt

      - name: Install lightweight CI dependencies
        run: |
          python -m pip install build ruff==0.12.10 mypy==1.18.2
          python -m pip install -e ./packages/transcript-postprocess -e .

      - name: Run repository test suite
        run: make test

      - name: Verify standalone CLI help
        run: |
          python -m whisperx_daemon --help
          python -m whisperx_daemon.standalone --help
          python -m transcript_postprocess --help

  quality:
    name: Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            packages/transcript-postprocess/pyproject.toml
            constraints-runtime.txt
            constraints-dev.txt
            requirements-cpu.txt
            requirements-cuda.txt
            requirements-dev-cpu.txt
            requirements-dev-cuda.txt

      - name: Install lightweight CI dependencies
        run: |
          python -m pip install build ruff==0.12.10 mypy==1.18.2
          python -m pip install -e ./packages/transcript-postprocess -e .

      - name: Run Ruff lint checks
        run: make lint

      - name: Run Ruff formatting checks
        run: make format-check

      - name: Run mypy
        run: make typecheck

  package:
    name: Package Build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build frontend
        run: python -m pip install build

      - name: Build whisperx-daemon distribution
        run: python -m build .

      - name: Build transcript-postprocess distribution
        working-directory: packages/transcript-postprocess
        run: python -m build .

  docker:
    name: Docker Build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build container image
        run: docker build -t whisperx-daemon:ci .
